/**
 * The explainability panel: the DSP features behind the current window.
 *
 * Shows spectral flatness, HF-energy ratio, jitter and shimmer against their
 * typical natural-speech ranges. This is what turns "the box said 82" into
 * "the box said 82 because the spectrum is flat and the pitch is too steady".
 */
export function EvidencePanel(_props: { detail: Record<string, number> }) {
  throw new Error("not implemented");
}
